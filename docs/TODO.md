# TODO:

- 清空缓存并恢复默认，放在上面，和所有计算在本地浏览器完成,并把上部房贷参谋这个表头区域高度压缩为很窄就行。“已开启本地缓存，刷新页面后会自动恢复当前输入。”可以在清空缓存按钮上悬浮提醒。
- 等额本息、等额本金 增加一个悬浮解释
- 贷款年限、年利率 使用滑块组件，注意最大、最小值
- 去掉 “当前方案下，月供约为 4,529 元，总利息约为 540,580 元，预计将在 2049-07-15 还清。” 模块
- 还款明细：
 - 如果没有还款历史，则隐藏“真实还款明细”模块
 - “以下明细已纳入历史利率变化、已发生提前还款和违约金。” 缩小字号 并移动到“真实还款明细”后面
- 历史变动： 
 - 事件不使用输入框组件，新年利率 改为 年利率（%） 并使用拖动滑块组件，把行高调整窄一些，或整个使用一个table
	- 应该增加违约金输入项
- 提前还款：
 - 减少年限 和减少月供 是否可以放在一个table内
 - 利率调整：
  - 新年利率 去掉 “新” 字，并使用滑块组件
 - 方案对比：
  - 当前方案下月供 金额不对，不是当前的，并检查其他数据。 提前还款-减少年限	提前还款-减少月供	利率调整 都写上具体的设置数据
  - 优化增体的日期选择组件样式
  - 网页增加主题随系统改变，但前端不用增加手动修改主题功能